import { Layout } from "@/components/Layout.tsx";

const Settings = () => {
  return (
    <Layout>
      <h1 className="text-3xl font-bold tracking-tight">Settings</h1>
      <p className="text-muted-foreground">Manage application settings here.</p>
    </Layout>
  );
};

export default Settings;